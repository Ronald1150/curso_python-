## Crear una funcion que resciba por argumentos  n numeros  y remote una lista con los numeros pares 
def num_pares(*args):
    lista_pares=[]
    for n in args:
        if n%2==0:
            lista_pares.apppend(n)
    return lista_pares
# por comprencion
#return [n for n in args if n%2==0]
print(num_pares(8,5,4,7,9,25,4,7,12))

#crear una funcion para los siguientes casos
#calcular el numero menor
#calcular el numero mayor
#calcular la suma de todos los numero
#se le pasara por argumentos n numeros
def Min(*args)
    menor=args[0]
    for n in args:
        if n<menor:
            menor





#Crear  una solucion que cambien el campo de semestre de 2 a 3
lista_alumnos=[
    {
        "nom"
    }
]
def objeto(e)


alumnos_actualizados=list(map(objeto,lista_alumnos))
print(alumnos_actualizados)
# devolver los numeros pares de una lista
lista=[4,8                                                                                                                                 ]



